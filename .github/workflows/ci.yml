name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  test-node:
    name: Run tests
    runs-on: macos-latest
    permissions:
      pull-requests: write
      contents: read
      checks: write
      id-token: write
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v4
        with:
          node-version: latest
      - run: npm ci
      - run: npx playwright install
      - run: npm test
